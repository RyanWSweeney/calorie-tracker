import React from "react";
import UserInfo from "./userInfo";

export const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <UserInfo/>
        </div>
    );
}

export default Dashboard;